node_modules
.DS_Store
dist
coverage
dist-ssr
*.local
.env
node_modules/*
coverage.env
lib/
# Firebase Admin SDK service account keys — NEVER commit
tribe-y-key.json
*-firebase-adminsdk-*.json
*-key.json

# Solana devnet keypair secret files — NEVER commit
reviewer-devnet.json
*-devnet.json

# Firebase emulator debug log
firebase-debug.log
